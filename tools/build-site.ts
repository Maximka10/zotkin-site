import { cpSync, existsSync, mkdirSync, readdirSync, rmSync, writeFileSync } from 'node:fs';
import { join } from 'node:path';
import { pathToFileURL } from 'node:url';
type PageModule = { html?: unknown };
const stripLegacyInlineUiScript = (html: string): string => html.replace(/\s*<script>\s*document\.addEventListener\([\s\S]*?var burger\s*=\s*document\.querySelector\(['"]\.burger['"]\)[\s\S]*?<\/script>/i, '');
const injectEnhancementScripts = (html: string): string => { let result = html; if (!result.includes('ui-refresh.js')) result = result.replace(/<\/body>/i, '  <script src="ui-refresh.js" defer></script>\n</body>'); if (!result.includes('site-enhance.js')) result = result.replace(/<\/body>/i, '  <script src="site-enhance.js" defer></script>\n</body>'); return result; };
const main = async (): Promise<void> => { const root = process.cwd(); const dist = join(root, 'dist'); const pagesDir = join(root, 'src', 'pages-ts'); if (!existsSync(pagesDir)) throw new Error('src/pages-ts is missing.'); rmSync(dist, { recursive: true, force: true }); mkdirSync(dist, { recursive: true }); const pageFiles = readdirSync(pagesDir).filter((file) => file.endsWith('.ts')).sort(); if (!pageFiles.length) throw new Error('No TypeScript pages were found.'); for (const file of pageFiles) { const page = (await import(pathToFileURL(join(pagesDir, file)).href)) as PageModule; if (typeof page.html !== 'string' || !page.html.includes('<!DOCTYPE html>')) throw new Error(`Invalid page module: ${file}`); writeFileSync(join(dist, file.replace(/\.ts$/i, '.html')), injectEnhancementScripts(stripLegacyInlineUiScript(page.html)), 'utf8'); } const copy = (source: string, destination: string): void => { if (!existsSync(source)) return; mkdirSync(destination, { recursive: true }); for (const entry of readdirSync(source)) cpSync(join(source, entry), join(destination, entry), { recursive: true }); }; copy(join(root, 'src', 'styles'), dist); copy(join(root, 'src', 'assets', 'images'), dist); copy(join(root, 'src', 'assets', 'icons'), dist); copy(join(root, 'public'), dist); const cname = join(root, 'CNAME'); if (existsSync(cname)) cpSync(cname, join(dist, 'CNAME')); console.log(`Generated ${pageFiles.length} static HTML pages from TypeScript.`); };
void main().catch((error: unknown) => { console.error(error); process.exitCode = 1; });
